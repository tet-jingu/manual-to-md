# MELSEC iQ-R Ethernetユーザーズマニュアル(応用編) — 2.3 応用設定 (x: データ交信用のタイマ設定 — 設定時の注意事項・リトライ回数)

| 項目 | 内容 |
|---|---|
| 変換範囲 (本ファイル) | 設定時の注意事項(Ethernet搭載ユニット側/相手機器側の各タイマ関係式・TCP分割送信回数の例2件)・リトライ回数(計算式・タイミングチャート) (原本 p.383-384 / PDFページ385-386) |
| 変換日 | 2026-09-20 |
| 変換方法 | PyMuPDF目視(PDF p.385,386を全ページrenderして不等式・タイミングチャートを直接確認して転記) |
| ページ対応 | PDFページ = 原本印刷ページ + 2 |
| 関連ファイル | 前 `76_2-3w_...`、続き `78_2-3y_...`(セキュリティ) |

> このファイルは原本PDFの参照用リファレンスです。数式・図は**原文のまま全文**転記しています。
> 未変換の範囲は `00_索引_ladder_reference.md` の変換範囲表を見てください。**設計判断をするときは、
> 最終確認を原本の該当ページで行ってください。**

## 目次

- [設定時の注意事項](#設定時の注意事項-原本-p383)
  - [Ethernet搭載ユニット側の各タイマの設定値](#ethernet搭載ユニット側の各タイマの設定値-原本-p383)
  - [相手機器側の各タイマの設定値](#相手機器側の各タイマの設定値-原本-p383)
- [リトライ回数](#リトライ回数-原本-p384)

---

## 設定時の注意事項 (原本 p.383)

### Ethernet搭載ユニット側の各タイマの設定値 (原本 p.383)

Ethernet搭載ユニット側の各タイマの設定値は，次の関係となる値で設定してください。

```
[レスポンス監視タイマ値] ≥ [TCP ULPタイマ値] ≥ [TCP終了タイマ値] ≥ [TCP再送タイマ値] > [IP組立てタイマ値]

[TCP再送タイマ値] = [TCPゼロウィンドウタイマ値]
```

なお，弊社品と回線接続するときは，双方のユニットに同じ設定を行ってください。

### 相手機器側の各タイマの設定値 (原本 p.383)

相手機器側の各タイマの設定値は，次の関係となる値で設定してください。各タイマ値の大小関係が下記でない場合は，送信タイムアウトなどの交信異常の発生頻度が高くなることがあります。

```
[相手機器側のTCP ULPタイマ値] > [Ethernet搭載ユニット側のTCP再送タイマ値]

[相手機器側アプリケーションソフトウェアでの監視タイマ値] > { [Ethernet搭載ユニット側のTCP ULPタイマ値] × n*1 }
```

\*1 nはTCP分割送信回数であり，下記計算により求められます。

```
n = ⌈ Ethernet搭載ユニットが送信する伝文サイズ ÷ Maximum Segmentサイズ ⌉ の小数点以下切上げ
```

**例: 同一回線上で交信する場合のTCP分割送信回数**

Maximum Segmentサイズは，同一回線上(ルータを介さない)では1460バイトとなり，TCP分割送信回数は下記になります。

- Ethernet搭載ユニットの送信伝文サイズ≤1460バイトのとき，n=1
- 1460バイト<Ethernet搭載ユニットの送信伝文サイズのとき，n=2

**例: 別回線上で交信する場合のTCP分割送信回数**

Maximum Segmentサイズは，別回線上(ダイアルアップルータ経由など)では最小536バイトとなり，TCP分割送信回数は下記になります。

- Ethernet搭載ユニットの送信伝文サイズ≤536バイトのとき，n=1
- 536バイト<Ethernet搭載ユニットの送信伝文サイズ≤1072バイトのとき，n=2
- 1072バイト<Ethernet搭載ユニットの送信伝文サイズ≤1608バイトのとき，n=3

## リトライ回数 (原本 p.384)

ノイズの影響などにより交信異常になるときは，リトライ回数が多くなるよう設定値を変更してください。リトライ回数は，次の計算式で決定されます。(デフォルトの場合，3=30÷10)

```
リトライ回数 ＝ TCP ULPタイマ値 ÷ TCP再送タイマ値
```

**例**

リトライ回数が3回になる設定値の場合，データ送信できなかったときは，下記のタイミングでデータ送信異常になります。(固定バッファ交信時)

【図】リトライ動作タイミングチャート(原本 p.384、固定バッファ交信時)
- データコマンド送信後、BUFSND命令がON継続。
- RJ71EN71: データコマンド送信 → (A: TCP再送タイマ値経過) → 1回目のリトライ → (A) → 2回目のリトライ → (A) → 3回目のリトライ
- 3回目のリトライ後、TCP ULPタイムアウト値に到達した時点でBUFSND命令完了デバイス+1が1スキャンだけONし、異常を通知する。
- A: TCP 再送タイマ値(データ送信後に”ACK”が返信されない場合に，データ送信する時間)

上記のリトライ処理をなくす(0回にする)には，下記の設定を行ってください。

```
TCP ULPタイマ値 ＝ TCP終了タイマ値 ＝ TCP再送タイマ値
```

(各タイマ値を同じにします)

---

## 図・原本参照が必要な項目の一覧 (原本 p.383-384)

| 項目 | 原本ページ | 扱い |
|---|---|---|
| リトライ動作タイミングチャート | p.384 | render目視で確認し文章化済み |

## 抜けチェック結果 (本md管理用 / 原本外)

原文(PDF p.385-386)とmdを突き合わせ、欠落なし。タイマ関係式・TCP分割送信回数の例(2件)、リトライ回数の計算式はすべてrender目視で確認のうえ全件転記。これでデータ交信用のタイマ設定(p.382-384)の変換が完了。
