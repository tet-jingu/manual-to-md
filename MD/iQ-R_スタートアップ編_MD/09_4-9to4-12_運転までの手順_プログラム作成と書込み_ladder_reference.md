# MELSEC iQ-R CPUユニットユーザーズマニュアル(スタートアップ編) — 4.9〜4.12 運転までの手順(プログラムの作成〜シーケンサへの書込み)

| 項目 | 内容 |
|---|---|
| 変換範囲 (本ファイル) | 4.9 プログラムの作成／4.10 プログラムの変換／4.11 プロジェクトの保存／4.12 シーケンサへの書込み (原本印刷ページ 74-78 / PDF p.76-80) |
| 変換日 | 2026-09-20 |
| 変換方法 | PyMuPDF目視(render確認済み。p.76-80全ページ)。プログラム例のラダー図(原本p.74)はrenderで目視し、ニモニックに書き起こした |

> このファイルは原本PDFの参照用リファレンスです。手順・注意事項は原文のまま全文転記しています。
> プログラム例(スタート1/ストップ1/タイマ1/ランプ1)は使用デバイス表・ニモニック・動作説明を全文記載しています。
> 未変換の範囲は `00_索引_ladder_reference.md` の変換範囲表を見てください。

## 目次

- [4.9 プログラムの作成](#49-プログラムの作成-原本p74-77)
  - [プログラム例](#プログラム例-原本p74)
  - [ラベルの登録](#ラベルの登録-原本p74-75)
  - [回路部品の挿入](#回路部品の挿入-原本p75)
  - [FBを挿入する場合](#fbを挿入する場合-原本p76)
  - [FB/FUNの作成について](#fbfunの作成について-原本p76)
  - [キー入力による回路の入力](#キー入力による回路の入力-原本p77)
- [4.10 プログラムの変換](#410-プログラムの変換-原本p78)
- [4.11 プロジェクトの保存](#411-プロジェクトの保存-原本p78)
- [4.12 シーケンサへの書込み](#412-シーケンサへの書込み-原本p78)

---

## 4.9 プログラムの作成 (4.9 / 原本p.74 / PDF p.76。render確認済み)

プログラムを作成します。下記のサンプルプログラムを例に，作成方法を示します。

### プログラム例 (原本p.74)

【図】プログラム例のラダー図(原本p.74。render確認済み)。以下にニモニックとして書き起こす。

#### 使用デバイス表(ラベル)

| ラベル名 | データ型 | 備考 |
|---|---|---|
| スタート1 | ビット | a接点。ラベルエディタで"ビット"を選択して登録(原本p.74の登録手順で明示) |
| ストップ1 | ビット | b接点。他のラベルと同様の手順で登録(原本記載) |
| タイマ1 | タイマ | b接点として条件に使用、かつOUT命令でプリセット値1000を設定。モニタ画面(原本p.81)で現在値表示"タイマ1 28"を確認 |
| ランプ1 | ビット | コイル出力、かつ自己保持のa接点として使用 |

> 要確認: ストップ1・タイマ1・ランプ1の詳細なデータ型登録画面は原本に明示的な画面キャプチャがなく、「プログラム例にある他のラベルも同様に登録します」との記載のみ。スタート1と同様にビット(タイマ1のみタイマ型)として登録されるものと判断した。

#### ニモニック(回路図から書き起こし。原本はラダー図のみでニモニックの記載なし)

```
LD    スタート1
OR    ランプ1
ANI   ストップ1
ANI   タイマ1
MPS
OUT   ランプ1
MPP
OUT   タイマ1    K1000
END
```

> 要確認: 原本にはラダー図(図示)のみが掲載されており、ニモニック表記そのものは原本に記載されていない。上記は原本のラダー図(スタート1のa接点起動、ランプ1のa接点による自己保持、ストップ1のb接点とタイマ1のb接点によるアンド条件、その条件でランプ1コイルとタイマ1(プリセット値1000)を並列駆動)を、GX Works3の標準的なニモニック変換規則(並列出力にMPS/MPPを使用)に基づいて変換者が書き起こしたものです。原本のラダー図自体(原本p.74)で最終確認してください。

動作説明(原本記載、全文):

- スタート1がONすると，タイマ1が計測を開始し，ランプ1がONします。
- タイマ1の現在値が1000になると，ランプ1がOFFします。
- ストップ1がONすると，ランプ1がOFFします。

### ラベルの登録 (原本p.74-75)

ラベルとは，名前やデータ型を任意に宣言可能な変数です。

プログラムにラベルを使用すると，デバイスやバッファメモリアドレスを意識することなくプログラムを作成できます。そのため，ラベルを使用したプログラムはユニット構成が異なるシステムでも簡単に再利用できます。

プログラム例で使用するラベルを，ラベルエディタから登録する方法を示します。

[ナビゲーションウィンドウ]⇨[プログラム]⇨[スキャン]⇨[MAIN]⇨[ProgPou]⇨[ローカルラベル]

#### 登録手順

プログラム例にある，"スタート1"のラベルの登録手順を示します。プログラム例にある他のラベルも同様に登録します。

【図】"ProgPou[PRG][ローカルラベル設定]"画面(ラベル名(1)欄、データ型ボタン(2))、および"データ型選択"画面(対象:すべて、データ型:ビット選択)。詳細は原本 p.74 を参照。

1. "ラベル名"の欄(1)に，"スタート1"を入力します。
2. "データ型"の欄の右にあるボタン(2)をクリックして，"データ型選択"画面を表示します。
3. ラベルのデータ型を指定します。この例では"ビット"を選択して，[OK]ボタンをクリックします。

> Point
> - ラベルエディタの[詳細表示]ボタンをクリックすると，ラベルのクラス，初期値，定数が設定できます。
> - ラベルエディタを使用せずに，プログラム作成時にラベルを登録することもできます。(4.9 キー入力による回路の入力)
> - グローバルラベルには，任意のデバイスを割り付けることができます。グローバルラベルのラベルエディタを開き，"割付け(デバイス/ラベル)"の項目に割り付けたいデバイスを入力してください。

### 回路部品の挿入 (原本p.75。render確認済み)

回路部品を，ドラッグ&ドロップでラダーエディタ上に配置して，プログラムを作成します。

ラダーエディタは，下記から表示します。

[ナビゲーションウィンドウ]⇨[プログラム]⇨[スキャン]⇨[MAIN]⇨[ProgPou]⇨[プログラム本体]

#### 挿入手順

プログラム例にある，"スタート1"のa接点の挿入手順を示します。プログラム例にある他の部品も同様に挿入します。

【図】部品選択ウィンドウから"LD[1]"をラダーエディタへドラッグ&ドロップする様子、"回路入力"画面(デバイス/ラベルの"s"欄にスタート入力、候補からスタート1選択)、挿入結果のラダー行。詳細は原本 p.75 を参照。

1. 部品選択ウィンドウから部品を選択して，ラダーエディタの配置したい箇所へドラッグ&ドロップ(1)します。この例では"LD[1]"をラダーエディタに配置します。
2. 挿入した部品をダブルクリックし，表示された画面の[拡張表示]ボタンをクリックします。
3. "回路入力"画面で，オペランドの指定をします。この例では"デバイス/ラベル"の"s"の欄(2)に"スタート"を入力します。
4. 表示された候補(3)から，入力したい項目を選択します。この例では"スタート1"を選択します。
5. "スタート1"のa接点がプログラムに挿入されます。

> Point
> - 接点やコイルなどの共通命令，汎用ファンクション/ファンクションブロックとユーザー定義FB/FUNは，部品選択ウィンドウの[部品一覧]タブ(1)を選択すると表示されます。"表示対象"のプルダウンメニュー(2)を使用して，配置したい部品を絞り込んでから，選択してください。
> - ユニットラベルとユニットFBは，部品選択ウィンドウの[ユニット]タブ(3)を選択すると表示されます。パラメータの設定時に追加したユニットラベルや，使用しているユニットのユニットFBを，ドラッグ&ドロップでエディタに配置することで，効率的にプログラムを作成できます。

### FBを挿入する場合 (原本p.76。render確認済み)

FBを挿入する場合の手順を示します。

【図】部品選択ウィンドウ[ユニット]タブからFB(M+RD62_CountEnable等)をラダーエディタへドラッグ&ドロップ、"FBインスタンス名入力"画面(ローカルラベル(ProgPou)選択、インスタンス名入力)、変換後の罫線接続結果。詳細は原本 p.76 を参照。

1. 部品選択ウィンドウから挿入したいFBを選択して，配置したい箇所へドラッグ&ドロップします。
2. "FBインスタンス名入力"画面が表示されます。インスタンスをグローバルラベルまたはローカルラベルのどちらに登録するか選択し，インスタンス名を入力します。
3. メニューの[変換]⇨[変換]を選択します。回路が変換され，FBインスタンスの入力ラベルおよび出力ラベルに罫線が接続されます。
4. 挿入したFBの入力回路部，出力回路部を作成してプログラムを完成させます。FBの入力回路部，出力回路部については下記を参照してください。

📖 各ユニットのFBリファレンス

#### FB/FUNの作成について (原本p.76)

ユーザー定義FB/FUNは，プログラムに挿入する前に，あらかじめ作成しておく必要があります。

【図】ナビゲーションウィンドウの"FB/FUN"(1)を右クリックして"データ新規作成"(2)を選択、"データ新規作成"画面(データ型: ファンクションブロック/データ名: FbPou、プログラム言語: ラダー等の詳細設定)。詳細は原本 p.76 を参照。

1. ナビゲーションウィンドウの"FB/FUN"(1)で右クリックし，"データ新規作成"(2)を選択します。
2. FBを作成する場合は"データ型"のプルダウンリストで"ファンクションブロック"を，ファンクションを作成する場合は"ファンクション"を選択して，[OK]ボタンをクリックします。
3. "プログラム本体"でFB/FUNの処理内容を作成します。

### キー入力による回路の入力 (原本p.77。render確認済み)

キー入力でも回路を作成できます。

#### 入力手順

プログラム例にある，"スタート1"のa接点の入力手順を示します。

【図】キー入力欄に"スタート"を入力し候補から"スタート1"を選択する様子、挿入結果のラダー行。詳細は原本 p.77 を参照。

1. 入力位置をクリックし，F5を押下します。
2. 入力欄(1)に"スタート"を入力します。キー入力に当てはまる候補(2)が表示されるので，"スタート1"を選択します。
3. "スタート1"のa接点がプログラムに表示されます。

> Point
> 回路の入力中に，ラベルを新規登録できます。"回路入力"画面に，登録したいラベルの名称を入力して，[OK]ボタンをクリックします。"未定義ラベル登録"画面で，ラベルの登録先，クラス，データ型を選択して，[OK]ボタンをクリックするとラベルが新規登録されます。

【図】"未定義ラベル登録"画面(ラベル名: スタート1、登録先: ローカルラベル(ProgPou)、クラス: VAR、データ型: ビット、定数値、コメント欄)。詳細は原本 p.77 を参照。

## 4.10 プログラムの変換 (4.10 / 原本p.78 / PDF p.80。render確認済み)

入力した回路ブロックの内容を確定します。

### 操作手順

1. メニューの[変換]⇨[変換]を選択します。
2. 変換を実施すると入力した回路が確定し，完了すると画面のグレー表示が白色になります。

## 4.11 プロジェクトの保存 (4.11 / 原本p.78 / PDF p.80)

作成したプロジェクトを保存します。

[プロジェクト]⇨[名前を付けて保存]

## 4.12 シーケンサへの書込み (4.12 / 原本p.78 / PDF p.80。render確認済み)

設定したパラメータと，作成したプログラムをCPUユニットに書き込みます。

[オンライン]⇨[シーケンサへの書込み]

### 操作手順

- CPUユニットを動作させるためには，システムパラメータ，CPUパラメータ，プログラムファイルの書込みが必要です。入出力ユニットやインテリジェント機能ユニットを動作させるためには，ユニットパラメータやユニット拡張パラメータの書込みが必要です。
- パラメータを新規設定・変更した場合は，CPUユニットをリセットしてください。(4.13 CPUユニットのリセット)
- [お気に入り選択]ボタンを使用することで，システムパラメータ/CPUパラメータやプログラムなど，よく使う項目を簡単に選択できます。[お気に入り選択]ボタンに設定する項目は，"オンラインデータ操作"画面の[設定]⇨[お気に入り選択の登録]から設定します。

1. "オンラインデータ操作"画面で，システムパラメータ，CPUパラメータ，ユニットパラメータ，プログラムファイルを選択します。FBを使用している場合は，該当するFB/FUNファイルも選択します。
2. [実行]ボタンをクリックします。
3. シーケンサへの書込みが完了したら，[閉じる]ボタンをクリックします。

## 図・原本参照が必要な項目の一覧

| 図 | 状態 |
|---|---|
| プログラム例のラダー図 (原本p.74) | render確認済み。ニモニックに書き起こし済み(要確認: 原本自体はラダー図のみでニモニック表記なし。変換者による標準規則での書き起こし) |
| ラベルエディタ・データ型選択画面 (原本p.74) | render確認済み |
| 回路部品挿入手順の画面3種 (原本p.75) | render確認済み |
| FB挿入手順の画面3種、FB/FUN作成画面2種 (原本p.76) | render確認済み |
| キー入力回路入力の画面2種、未定義ラベル登録画面 (原本p.77) | render確認済み |
